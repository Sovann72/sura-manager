name: sura_manager
description: ValueNotifier and ValueListenableBuilder but for asynchronous value.
version: 0.2.1
homepage: https://github.com/asurraa/sura_manager

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.0
  sura_flutter: ^0.5.1

dev_dependencies:
  flutter_lints:
  flutter_test:
    sdk: flutter
flutter:
